<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Timepickerexample"
             x:Class="Timepickerexample.MainPage">

    <!--<VerticalStackLayout>
        <Label Text="Embedded TimePicker"
               FontSize="24"
               HorizontalOptions="Center" />

        -->
    <!-- The embedded TimePicker -->
    <!--
        <local:CustomTimePicker x:Name="EmbeddedTimePicker" SelectedTime="{Binding SelectedTime}" />
    </VerticalStackLayout>-->
    <VerticalStackLayout Padding="20">
        <Label Text="TimePicker with Spinner Style (Android)"
               FontSize="24"
               HorizontalOptions="Center" />
        <TimePicker x:Name="timePicker" />

        <Label Text="Inline TimePicker"
               FontSize="24"
               HorizontalOptions="Center" />
        <local:InlineTimePicker />


        <Label Text="Inline TimePicker"
               FontSize="24"
               HorizontalOptions="Center" />

        <local:InlineTimePicker x:Name="InlineTimePicker" />

        <Button Text="Get Time"
                Clicked="OnGetTimeClicked" />
    </VerticalStackLayout>
</ContentPage>
